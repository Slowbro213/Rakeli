
export const terminalInput = document.getElementById("terminal-input") as HTMLInputElement;

export const terminalInputRow = document.getElementById("terminal-input-row") as HTMLInputElement;

export const autocomplete = document.getElementById("autocomplete") as HTMLElement;

export const terminalBody = document.getElementById("terminal-body") as HTMLElement;

export const template = document.getElementById("terminal-line-template") as HTMLTemplateElement;

